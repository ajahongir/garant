<ul>
  <% if Admin::TargetsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_targets_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_target_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::TargetsController.sortable? and Target.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Targets"),
                 admin_targets_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Targets"),
                 admin_targets_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>